import {
  init_esm_shims
} from "./chunk-WWCIODDN.js";

// Events/Admin/devTaskDel.ts
init_esm_shims();
import { Events } from "discord.js";
var devTaskDel_default = {
  name: Events.MessageCreate,
  async execute(message) {
    if (message.channelId != "1200374840696246302") return;
    if (message.embeds.length != 0) return;
    setTimeout(async () => {
      const msg = await message.channel.messages.fetch(message.id);
      if (!msg) return;
      else if (msg && !msg.pinned && msg.deletable) {
        await msg.delete().catch((err) => {
          console.log(err);
        });
      } else return await msg.react(":name_badge:");
    }, 5e3);
  }
};
export {
  devTaskDel_default as default
};
