import {
  init_esm_shims
} from "./chunk-WWCIODDN.js";

// Buttons/ticket/clan.ts
init_esm_shims();
import { ActionRowBuilder, ModalBuilder, TextInputBuilder, TextInputStyle } from "discord.js";
var clan_default = {
  id: "clan-ticket",
  async execute(interaction) {
    const modal = new ModalBuilder({
      title: "Einem Clan beitreten",
      customId: "clan-entry"
    });
    const field = new TextInputBuilder({
      customId: "clan",
      label: "In welchen Clan m\xF6chtest du?",
      placeholder: "Bitte schreib hier das Clank\xFCrzel",
      required: true,
      style: TextInputStyle.Paragraph
    });
    const row = new ActionRowBuilder({ components: [field] });
    modal.addComponents(row);
    await interaction.showModal(modal);
  }
};
export {
  clan_default as default
};
