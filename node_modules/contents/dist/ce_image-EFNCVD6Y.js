import {
  init_esm_shims
} from "./chunk-WWCIODDN.js";

// Buttons/CustomEmbed/ce_image.ts
init_esm_shims();
import { ModalBuilder, TextInputBuilder, ActionRowBuilder } from "discord.js";
var ce_image_default = {
  id: "ce_image",
  async execute(interaction) {
    if (interaction.user.username != interaction.message.embeds[0].author.name) return interaction.reply({ content: "Du darfst an diesem Embed nichts \xE4ndern", ephemeral: true });
    const modal = new ModalBuilder({
      customId: "ce_image",
      title: "Bild \xE4ndern"
    });
    const image = new TextInputBuilder({
      label: "Gib hier die URL f\xFCr das Bild ein",
      minLength: 1,
      customId: "url",
      required: true,
      style: 2
    });
    const row = new ActionRowBuilder().addComponents(image);
    modal.addComponents(row);
    await interaction.showModal(modal);
  }
};
export {
  ce_image_default as default
};
