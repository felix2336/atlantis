import {
  init_esm_shims
} from "./chunk-WWCIODDN.js";

// Commands/General/confession.ts
init_esm_shims();
import { SlashCommandBuilder, ModalBuilder, ActionRowBuilder, TextInputBuilder } from "discord.js";
var command = {
  data: new SlashCommandBuilder().setName("beichte").setDescription("Erstelle eine anonyme Beichte"),
  async execute(interaction, client) {
    const modal = new ModalBuilder({
      title: "Anonyme Beichte",
      customId: "confession",
      components: [
        new ActionRowBuilder({
          components: [
            new TextInputBuilder({
              customId: "confession",
              label: "Deine Beichte",
              required: true,
              style: 2
            })
          ]
        })
      ]
    });
    await interaction.showModal(modal);
  }
};
var confession_default = command;
export {
  confession_default as default
};
