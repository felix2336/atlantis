import {
  init_esm_shims
} from "./chunk-WWCIODDN.js";

// Buttons/ticket/cr-decline.ts
init_esm_shims();
import { Colors, EmbedBuilder } from "discord.js";
var cr_decline_default = {
  id: "cr_decline",
  async execute(interaction) {
    if (interaction.user.username != interaction.channel.name.split("-")[1]) return interaction.reply({ content: "Nur der Ersteller des Tickets darf diesen Button dr\xFCcken!", ephemeral: true });
    const embed = new EmbedBuilder({
      title: "Schlie\xDF-Anfrage",
      description: `${interaction.member} hat die Schlie\xDF-Anfrage abgelehnt!`,
      color: Colors.Red
    });
    await interaction.message.edit({ content: "", embeds: [embed], components: [] });
    await interaction.reply({ content: "Du hast die Schlie\xDF-Anfrage abgelehnt!", ephemeral: true });
  }
};
export {
  cr_decline_default as default
};
